services:

  web:
    build:
      context: /home/david/devops/Docker/Caronte
      dockerfile: ./dockerfiles/base/ubbase
      #args:
        # - USUARIO=alfonso 
    container_name: ctubbase
    environment:
      - USUARIO=alfonso
    volumes:
      - ./logs:/root/logs