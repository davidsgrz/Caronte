services:

  web:
    build:
      context: ${CONTEXTO} 
      dockerfile: ./dockerfiles/base/ubbase
      #args:
        # - USUARIO=alfonso 
    container_name: ${CONTENEDOR}

    env_file:
      - ./.env
    volumes:
      - ./logs:/root/logs